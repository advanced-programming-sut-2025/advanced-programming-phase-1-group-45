package com.proj.Model.inventoryItems.trees;

public class ForagingTree extends Tree {
    public ForagingTree(TreeData data, int x, int y) {
        super(data, x, y);
//        setCurrentStage(getTreeData().getDaysPerStage().length);
    }
}
